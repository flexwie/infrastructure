apiVersion: v1
kind: Service
metadata:
  name: traefik-ingress-service
  namespace: kube-system
spec:
  selector:
    k8s-app: traefik-ingress-lb
  ports:
  - port: 80
    protocol: TCP
    name: web
  - port: 8080
    protocol: TCP
    name: admin
  type: NodePort